# DOA5LR-Salons — Bug report

Copy the relevant fields, fill them in and attach only the diagnostic files needed for your issue. Use setup's **Logs → Export diagnostics ZIP...** to save a local archive. It is not uploaded automatically. Logs are not anonymized: inspect the ZIP/screenshots and remove sensitive names, Steam IDs, IP addresses or local paths before sharing. Do not include Steam credentials, authentication tokens or game saves.

## Setup

- Pack version:
- Installer version, if relevant:
- Windows version:
- Input mode: Keyboard / Controller / Keyboard + controller (experimental)
- Keyboard layout or controller model:
- USB or Bluetooth, if relevant:
- Steam Input enabled, disabled or unknown:
- Controller connected before game launch: yes / no
- Install method: full installer / portable controls app / manual pack
- Other input or game mods, if any:

## Problem

- Expected behavior:
- Actual behavior:
- Exact error text:
- Does it happen every time?
- Last version/setup that worked, if known:

## Steps to reproduce

1.
2.
3.

## Attachments

- Screenshot of the error or relevant screen.
- Relevant installer log or diagnostic archive, reviewed before sharing.
- For lobby/invite issues: `DOA5LR-Lobby.log` from the game folder or `scripts`, if present, captured after reproducing the issue.
- For a device profile issue: the device name and VID/PID shown by Controller Finder.
- For an antivirus alert: product name, detection name, affected filename and SHA-256, plus protection-history screenshot if useful.

If a log is missing, say so. This report does not require a copy of your entire game folder or Steam configuration.
